

buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:1.3.0'

    }
}

allprojects {
    repositories {
        jcenter()
        flatDir {
            dirs "libs"
        }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
